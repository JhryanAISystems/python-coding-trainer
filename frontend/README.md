# Frontend

React + TypeScript + Vite + Tailwind frontend for the Python Coding
Trainer. See the [project README](../README.md) for full setup,
architecture, and deployment instructions.

Quick start:

```bash
npm install
npm run dev
```

Runs at http://localhost:5173 and proxies `/api` requests to the backend
(see `vite.config.ts`).
